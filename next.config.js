/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.pinimg.com','images.pexels.com'], // Add more domains as needed
      },
}

module.exports = nextConfig
